{
	"language": "Solidity",
	"sources":
	{
		"A":
		{
			"content": "pragma solidity >=0.0; contract C { function f() external pure returns (string memory) { return \"abcabc\"; } }"
		}
	},
	"settings":
	{
		"outputSelection":
		{
			"*": { "*": ["ir"] }
		}
	}
}
